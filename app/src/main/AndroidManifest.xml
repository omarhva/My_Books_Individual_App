<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mybooks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".UpdateToekomstigActivity"></activity>
        <activity android:name=".UbdateHuidigBook" />
        <activity android:name=".BoekenToevoegenToekomstig" />
        <activity
            android:name=".ToekomstigBoekenActivity"
            android:label="@string/title_activity_toekomstig_boeken"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BoekenToevogenActivity" />
        <activity
            android:name=".HuidigBooksActivity"
            android:label="@string/title_activity_huidig_books"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>